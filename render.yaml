services:
  - type: web
    name: divvyup-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false  # Set to your Fly.io backend URL: https://<your-app-name>.fly.dev
